node_modules/
.DS_Store
*.log
dist/
build/
.env
archive/*.mp4
archive/*.zcrystal
vault/personal/z_request_access_private.pem

# Pytest (repo-local basetemp and cache; see pytest.ini)
.pytest_basetemp_root/
data/.pytest_cache/

# Super Chat local memory (created on first use; do not commit conversations)
data/ai-memory.json

# Z-Autonomous AI local approval token (optional; one line)
data/local/z_autonomous_approval_token

# Z-PAM Lite (user-owned local memory; do not commit)
.zpam/

# Generated transparency deck/PDF (regenerate: npm run report:ecosphere:pptx; keep exports/ as receipts locally)
data/reports/exports/*.pptx
data/reports/exports/*.pdf

# Mini sync bot — timestamped registry copies (re-run bot:sync to regenerate; optional local only)
data/reports/bot_sync_snapshots/

# MirrorSoul Slice 1 — user journal files (regenerate with local use only)
data/mirrorsoul/entries/
data/mirrorsoul/zes_state.json
data/mirrorSoul/entries.jsonl
data/mirrorSoul/reflections.jsonl
data/mirrorSoul/emotional_patterns.json
data/mirrorSoul/adaptive_fuel.jsonl
data/logs/z_prediction_validation.jsonl

# Zuno transformation flow — per-session JSON (local runs)
data/zunoFlow/sessions/

# AMK local safety ignores
node_modules/
.env
*.exe
*.zip
*.odt
*.pdf
uploads/
Z_Sanctuary_Universe 2/
$file
safe_pack/
vault/personal/
**/snapshots/
**/quarantine/
**/desktop.ini
lab-outputs/
vault/personal/
**/snapshots/
**/quarantine/
**/desktop.ini
lab-outputs/
data/reports/*.json
!data/reports/z_crystal_dna_drift_report.json
!data/reports/z_anydevice_simulation_report.json

# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by istanbul / nyc
coverage
*.lcov
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of `npm pack`
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env.*
!.env.example

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist
.output

# Gatsby files
.cache/

# vuepress build output
.vuepress/dist

# vuepress v2.x temp directory
.temp

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# pnpm
.pnpm-store

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/

data/reports/incidents/

# Z-DOORWAY-3 — local session telemetry (operator opt-in via -SessionLog); do not commit
data/reports/z_doorway_session_log.jsonl
