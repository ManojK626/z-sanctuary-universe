# Z-SME optional same-origin audio (`public/audio`)

Place **licensed or self-owned** demo tracks here when you are ready for v1.2 playback.

Suggested filenames (must match `audioUrl` in `src/lib/zMusicEngine/audioSources.js`):

- `aligned-with-nature-demo.mp3`
- `awakened-energy-demo.mp3`
- `inner-journey-demo.mp3`

**Rule:** do not set `audioUrl` in code until the file exists — avoid broken `<audio>` `src`.

See `z_music_engine/PHASE_2_8_Z_SME_SAME_ORIGIN_AUDIO_RECEIPT.md`.
