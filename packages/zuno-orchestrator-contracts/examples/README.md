# Zuno orchestrator contract examples (Phase 1B)

**Non-executable fixtures.** These JSON files illustrate shapes aligned with `src/*.ts` types. They are **not** consumed by a runtime orchestrator in this repo yet.

- Do not treat as API payloads for live providers.
- **Do not** pass these files to any task runner as executable plans — see [package README](../README.md) and [ZUNO_CONTRACT_EXAMPLES.md](../../../docs/orchestration/ZUNO_CONTRACT_EXAMPLES.md).
- Strip **`_*`** keys after parse if you ever assign parsed JSON to contract types.
- No secrets; all IDs are fictional.

See [docs/orchestration/ZUNO_CONTRACT_EXAMPLES.md](../../../docs/orchestration/ZUNO_CONTRACT_EXAMPLES.md) for field-by-field notes.
