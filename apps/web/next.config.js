// Z: apps\web\next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['z-sanctuary-mirrorsoul-slice', 'z-sanctuary-zuno-transformation-slice'],
};

export default nextConfig;
