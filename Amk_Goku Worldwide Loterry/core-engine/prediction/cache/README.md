<!-- Z: Amk_Goku Worldwide Loterry\core-engine\prediction\cache\README.md -->

# Prediction Cache

JSON files generated by `engine.py` land here (`{format}_predictions.json`). They record the lookback window, LPBS parameters, lines produced, and the CSV source timestamp. Reuse these files for fast UI displays or as training seeds for downstream AI modules.
