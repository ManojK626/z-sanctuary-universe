# Z-Sanctuary Follow-Up Instruction — Operational Technology Layers PR

## Current PR

Branch / PR lane: `copilot/z-operational-technology-layers-0`

Purpose: Operational Technology Layers Phase 0 builder-spine doctrine.

This PR now includes:

- `docs/Z_OPERATIONAL_TECHNOLOGY_LAYERS.md`
- `docs/PHASE_Z_OPERATIONAL_TECHNOLOGY_LAYERS_0_GREEN_RECEIPT.md`
- `.cursor/rules/z-operational-technology-layers.mdc`
- `.cursor/rules/z-sanctuary-core.mdc`
- `docs/INDEX.md`
- `docs/AI_BUILDER_CONTEXT.md`

## Main decision

Keep `.cursor/rules/z-sanctuary-core.mdc` in this PR only if it remains:

- short
- pointer-only
- non-duplicative
- no doctrine copy-paste blocks
- no runtime authority
- no auto-deploy language
- no hidden automation language

It is allowed because it supports the builder spine and points to:

- Hierarchy Chief
- Operational Technology Layers
- Turtle Mode
- 14 DRP
- PR discipline

## Required final checks

Before merge, confirm:

- No runtime files changed
- No dashboard HTML/JS/CSS changed
- No `data/reports/*`
- No package-lock churn
- No Cloudflare binding
- No deployment workflow
- No Z-ADTF runtime
- No LinguaCore files
- `npm ci` if dependencies are missing
- `npm run verify:md` PASS

## PR body update

Add this line to the PR body:

> Includes thin `.cursor/rules/z-sanctuary-core.mdc` builder index; pointer-only, no duplicated doctrine, no runtime authority.

## Merge order

Keep this order:

1. MD060 hygiene
2. Operational Technology Layers PR
3. LinguaCore Phase 0 / rebase Phase 1
4. Mauritius real-world device validation
5. Z-ADTF after lived receipts

## Cloudflare AI instruction

Cloudflare AI / Cloudflare tooling must remain inactive for this PR.

Do not add:

- Pages deployment
- Workers
- Tunnel
- Zero Trust config
- DNS changes
- environment variables
- AI bindings
- deployment scripts

Cloudflare remains future-phased only:
`Pages → Zero Trust review → Workers only with charter`

## Standing laws

```text
federation ≠ authority
topology ≠ ownership
observe → verify → suggest → human decides
readiness ≠ deploy
organization ≠ control
layered tools ≠ the soul
```

## Final instruction

This PR is doctrine-only. Review it as a builder-spine and governance-alignment PR, not as a runtime feature branch.
