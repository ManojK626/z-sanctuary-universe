# Baby / family projection — consent boundary

**Posture:** highly sensitive personal and family context.

## Rules

1. **Adults-only framing** where any fertility or relationship projection appears.
2. **Dual consent** if the feature involves more than one person’s data.
3. **No medical claims** — not a clinical tool; pair with `NON_MEDICAL_PRODUCT_BOUNDARY.md`.
4. **Data minimization** — collect only what the spec proves necessary.

## Registry

Pair with `soulmate_baby_predictor` (or successor id) in `data/z_master_module_registry.json`.
