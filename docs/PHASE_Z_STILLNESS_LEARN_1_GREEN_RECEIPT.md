# Phase Z-STILLNESS-LEARN-1 green receipt

## Delivered

- `docs/Z_STILLNESS_MODE_AI_LEARNING_PATHWAY.md` — doctrine and pathway levels.
- `docs/Z_SANCTUARY_ECOSYSTEM_BIOLOGY_MAP.md` — metaphor map for idle orientation.
- `data/z_stillness_learning_registry.json` — law, biology keys, outputs, AI team anchors.
- `data/examples/z_stillness_learning_samples.json` — GREEN / YELLOW / BLUE samples plus RED fixtures (env-flagged).
- `scripts/z_stillness_learning_check.mjs` — read-only validator and report writer.
- `data/reports/z_stillness_learning_report.{json,md}` — generated by the script.
- Indicator **`z_stillness_ai_learning_pathway`** + dynamic overlay **`z_stillness_learning`** in `dashboard/scripts/amk-project-indicators-readonly.js`.
- Hub links: `package.json` (`z:stillness:learn`), `docs/INDEX.md`, `docs/AI_BUILDER_CONTEXT.md`, `data/z_autonomy_task_policy.json`, and short cross-links in related governance docs.

## Acceptance

- Validator **never** starts daemons, schedules tasks, edits the repo automatically, merges, deploys, calls providers, or writes secrets.
- Exit code **1** **only** when the **overall signal** is **RED** (e.g. RED fixtures enabled or registry structure failure).
- Default run (fixtures off) exits **0** with GREEN/YELLOW/BLUE mixes as designed.
- `background_automation_enabled` in the JSON report stays **false** for Phase 1.

## Operator checks (suggested)

```bash
node -e "JSON.parse(require('fs').readFileSync('data/z_stillness_learning_registry.json','utf8')); console.log('Z Stillness learning registry OK')"
npm run z:stillness:learn
npm run verify:md
npm run dashboard:registry-verify
```

## Rollback

Remove stillness artefacts listed above, the npm script entry, autonomy policy row, indicator block + overlay fetch/apply, and cross-links added for this phase.
