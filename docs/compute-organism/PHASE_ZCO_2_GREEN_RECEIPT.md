# Phase ZCO-2 — Green Receipt (Compute Organism Status Layer)

**Phase:** ZCO-2 — read-only infrastructure awareness spine
**Scope:** Observer script + reports only — **no** hardware, orchestration, networking control
**Date:** 2026-05-21
**Prerequisite:** ZCO-1 doctrine pack + ZCO-1A MD060 stabilization (`npm run verify:md` exit 0)

## Intent

First governed **Infrastructure Awareness Spine** for Z-Compute Organism: validates doctrine presence, example JSON, swarm roles, forbidden-runtime absence, and rolls up hub traffic signal — **observe-only**.

## Deliverables

| Artifact | Status |
| ---------------------------------------------------- | ---------------------------------------------- |
| `scripts/z_compute_organism_status.mjs` | Added |
| `data/reports/z_compute_organism_status.json` | Generated by script |
| `data/reports/z_compute_organism_status.md` | Generated by script |
| `npm run z:compute:organism` | Wired in `package.json` |
| `.gitignore` exception | `!data/reports/z_compute_organism_status.json` |
| `docs/compute-organism/PHASE_ZCO_2_GREEN_RECEIPT.md` | Added |

## Script law

The observer **only**:

- reads doctrine markdown under `docs/compute-organism/`
- validates `data/examples/z_compute_node_registry.example.json` and `z_compute_swarm_roles.example.json`
- summarizes node and swarm example metadata
- audits `package.json` / `scripts/z_compute*.mjs` for forbidden runtime patterns
- reads `data/reports/z_traffic_minibots_status.json` for hub traffic signal
- writes `data/reports/z_compute_organism_status.{json,md}`

The observer **does not**: orchestrate, control hardware, scan networks, deploy, merge, install packages, or execute against devices.

## Acceptance

- [x] Report includes mode, posture (orchestration CLOSED, hardware DISABLED)
- [x] Doctrine presence for Arelium, SWARM, builder spine, formulas
- [x] Example validation PASS/FAIL with node and role counts
- [x] Forbidden runtime audit (only read-only `z_compute_*.mjs` observer/intake/draft under `scripts/z_compute*` — ZCO-5/6 allowlist)
- [x] `overall_signal` rollup with smallest safe next action
- [x] No new hardware or orchestration runtime beyond this read-only script

## Verification

```bash
npm run verify:md
npm run z:compute:organism
npm run z:traffic
npm run z:car2
```

Record `overall_signal` from compute report and `traffic_chief.overall_signal` from traffic JSON.

## Verification evidence (2026-05-21)

| Command | Exit | Signal / note |
| ---------------------------- | ---- | -------------------------------------------------------------- |
| `npm run verify:md` | 0 | Hub MD060 stable after `z_markdown_table_compact.mjs` (ZCO-1A) |
| `npm run z:compute:organism` | 0 | `overall_signal` **GREEN**, `examples_validated` **PASS** |
| `npm run z:traffic` | 0 | `overall_signal` **GREEN** |
| `npm run z:car2` | 0 | Scan complete |

## Rollback

Remove script, npm script, gitignore exception, reports, and this receipt. Re-run `npm run verify:md`.

## Locked law

```text
ZCO-2 = awareness spine only.
Status report ≠ permission to orchestrate.
GREEN ≠ deploy.
AMK-Goku owns sacred moves.
```
