# Phase Z-CRYSTAL-DNA-3 — Green Receipt

**Phase:** Z-CRYSTAL-DNA-3 — Shard Drift and Integrity Awareness
**Scope:** Read-only drift detection and reports; optional drift overlay on the DNA map.
**Date:** 2026-05-12

## Deliverables

| Artifact | Status |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `docs/Z_CRYSTAL_DNA_DRIFT_AWARENESS.md` | Added |
| `scripts/z_crystal_dna_drift_check.mjs` | Added |
| `data/reports/z_crystal_dna_drift_report.json` | Generated by script (git-tracked via `.gitignore` exception) |
| `data/reports/z_crystal_dna_drift_report.md` | Generated by script |
| `npm run z:crystal:dna:drift` | Wired in `package.json` |
| Optional map overlay | `dashboard/scripts/z-crystal-dna-map-readonly.js` + `dashboard/styles/z-crystal-dna-map.css` |

## Law confirmation

- No repair, no deploy, no git mutation, no NAS writes, no automatic restore, no secret access.
- Writes limited to drift report JSON and Markdown only.

## Verification (run from hub root)

Commands for this receipt:

```bash
npm run verify:md
npm run z:traffic
npm run z:car2
npm run z:crystal:dna:drift
```

Record stdout and exit codes in your overseer log when promoting posture.

## Verification evidence (this hub)

- `npm run verify:md` — exit **0** (markdownlint clean after `md:table-compact` on affected DNA docs tables).
- `npm run z:traffic` — exit **0**, `overall_signal` **GREEN** in `data/reports/z_traffic_minibots_status.json`.
- `npm run z:car2` — exit **0**, scan completed (`z_car2_similarity_report.json` / `.md` refreshed).
- `npm run z:crystal:dna:drift` — exit **0**, sample rollup **BLUE** with `findings_count` **3** in `data/reports/z_crystal_dna_drift_report.json` (data-driven; re-run after manifest edits).

## Sample drift outcome

After `npm run z:crystal:dna:drift`, inspect `data/reports/z_crystal_dna_drift_report.json` for `overall_signal`, `findings_count`, and `findings[]`. Rollup and severities are data-driven; remediate manifests and posture outside this script.
