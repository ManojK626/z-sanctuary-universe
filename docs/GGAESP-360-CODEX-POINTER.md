# GGAESP-360 — pointer (hub)

**Implementation:** [core_engine/ggaesp_pipeline.ts](../core_engine/ggaesp_pipeline.ts) (`runGGAESP`, v11–v19 chain) and [core_engine/ggaesp_memory.ts](../core_engine/ggaesp_memory.ts) (append-only capsules under `data/ggaesp/`).

**Sealed human spec + ideation (GGAESP + 360 Ring, lysisers, Z-ECR):** `Z_Sanctuary_Universe 2/docs/codex/GGAESP-360-SEALED.md` in the Organiser (continuation batch; path relative to sibling folder next to this hub).

**Dev heatmap / LPBS proof (no UI):** from hub root, `npm run ts:build:ggaesp` then `npm run ts:run:ggaesp:heatmap` (MEDIUM / PREPARE path) or `npm run ts:run:ggaesp:heatmap:go` (LOW / **GO** clean opportunity). Shims: `node core_engine/browser/ggaesp_heatmap_dev_sample.js` and `node core_engine/browser/ggaesp_heatmap_go_dev_sample.js`. Source: [heatmap_dev/](../core_engine/heatmap_dev/) → emit under [dist_heatmap/](../core_engine/dist_heatmap/).

**Governance:** Advisory scores and energy/build-gate rules do not override [AGENTS.md](../AGENTS.md) or product compliance; extend at **v20+** or named extensions, not by silently redefining v11–v19.
