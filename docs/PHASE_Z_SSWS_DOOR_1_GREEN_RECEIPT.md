# Phase Z-SSWS-DOOR-1 — green receipt (AMK Workspace Doorway)

**Date:** 2026-05-04
**Scope:** Local registry + read-only status script + PowerShell folder/workspace opener + reports + documentation + dashboard indicator metadata.
**Out of scope:** Deploy, server auto-start, `npm install`, extension auto-install, NAS mount, secrets, Cloudflare, provider calls, auto-merge, smoke/build chain execution from the doorway.

## Delivered

- `data/amk_workspace_doorway_registry.json` — schema `amk_workspace_doorway_registry_v1`, profiles, projects, laws.
- `scripts/amk_workspace_doorway_status.mjs` — path/workspace/Cursor probe, duplicate path note, NAS declaration-only note, reports.
- `scripts/amk_open_workspace_doors.ps1` — `-Profile`, `-DryRun`, `-OpenDashboard`; Cursor → VS Code → Explorer fallback.
- `data/reports/amk_workspace_doorway_status.{json,md}` — generated by the status script (commit after `npm run amk:doorway:status`).
- `docs/AMK_WORKSPACE_DOORWAY.md` — operator doctrine and ladder.
- `package.json` — `amk:doorway:status`, `amk:doorway:open`.
- `dashboard/data/amk_project_indicators.json` — indicator `amk_workspace_doorway` + overlay key.
- `dashboard/scripts/amk-project-indicators-readonly.js` — optional overlay from doorway report JSON.
- Cross-links in `docs/AI_BUILDER_CONTEXT.md`, `docs/Z_SSWS_WORKSPACE_SPINE.md`, `docs/Z_SSWS_LAUNCH_REQUIREMENTS_POLICY.md`, `docs/AMK_PROJECT_INDICATORS_AND_GO_NO_GO.md`, `docs/INDEX.md`.

## Verification (operator)

```bash
node -e "JSON.parse(require('fs').readFileSync('data/amk_workspace_doorway_registry.json','utf8')); console.log('AMK doorway JSON OK')"
npm run amk:doorway:status
powershell -ExecutionPolicy Bypass -File scripts/amk_open_workspace_doors.ps1 -Profile morning -DryRun
npm run verify:md
npm run z:ssws:requirements
npm run z:traffic
npm run z:car2
```

## Posture

- **Go/No-Go:** `NO_GO_FOR_AUTO_LAUNCH` — doorway is manual and local only.
- **AMK** retains sacred moves: merge, deploy, NAS credentials, and production lanes stay outside this phase.
