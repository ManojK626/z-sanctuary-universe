# Engine name (replace)

## Registry Identity

| Field | Value |
| ----------------- | ----------------------------------- |
| **Engine ID** | `engine_id_from_json` |
| **Registry file** | `data/z_core_engines_registry.json` |

## Role

One paragraph: what this engine represents in the architecture.

## Evidence (optional)

List real paths if this engine maps to concrete code or docs:

- `path/...`

If none:

> No per-engine code path listed here — see hub engines registry and Zuno audit for module-level evidence.

## Risk and posture

| Risk | Status from registry |
| --------- | ----------------------------------------------- |
| From JSON | `active_or_partial` / `doctrine_or_partial` / … |

## Cursor Builder Notes

- Do not treat registry presence as full production readiness.
- Cross-link modules that implement this engine when IDs are known.
