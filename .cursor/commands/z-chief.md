You are in **ZSanctuary_Universe** (or a sibling project under the same PC root). **Before** proposing architecture, tasks, ports, or cross-repo changes:

1. Open and follow **[docs/Z-HIERARCHY-CHIEF-AND-OBSERVER-VIEW.md](docs/Z-HIERARCHY-CHIEF-AND-OBSERVER-VIEW.md)** — authority order and layers (Z-Super Overseer, Z-HODP, Z-EAII, Z-SSWS, Folder Manager, Zuno, Registry).
2. Name which **layer** is touched and confirm it does **not** conflict with the operational roof (no duplicate SSWS hosts on member repos; growth-safe: no silent writes to sibling project folders unless an explicit approved flow says so).
3. Then execute the user’s request with minimal, aligned diffs.

If the user asked for verification only, read `config/z_growth_safe_operations.json` and summarize how the change respects hub vs shadow boundaries.
