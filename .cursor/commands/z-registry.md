Run **registry and structure verification** from the repo root (ZSanctuary_Universe), then summarize results for the user.

Execute in the terminal from the **workspace root** (the folder that contains `package.json` and `scripts/z_registry_omni_verify.mjs`):

```powershell
node scripts/z_registry_omni_verify.mjs
```

Report: pass/fail lines only, plus one sentence on PC root hub alignment.
