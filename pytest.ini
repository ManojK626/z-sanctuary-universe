[pytest]
# Keep basetemp and cache under the repo so Windows ACL issues on %TEMP% or
# locked .pytest_cache folders do not break tests that use tmp_path.
addopts = --basetemp=.pytest_basetemp_root
cache_dir = data/.pytest_cache
