# Shadow workbench (SKK + RKPK)

`index-skk-rkpk.workbench.html` is a **working copy** of the unified HODP shell. Edit here for experiments; when stable, merge changes into `../index-skk-rkpk.html`.

**NAV-1 parity:** This shadow file loads the same Universal Workstation Navigator assets as the canonical page (`../../styles/z-universe-navigator.css`, `../../scripts/z-universe-service-map.js`, `z-universe-navigator-readonly.js`) so workbench iteration includes the read-only service rail.

Registry for this pattern (and future surfaces): `config/z_dashboard_entrypoints.json`.
